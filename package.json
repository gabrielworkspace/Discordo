{
  "name": "discordo",
  "version": "1.0.0",
  "description": "Discordo MVP",
  "scripts": {
    "start": "npm run dev --prefix client",
    "dev": "npm run dev --prefix client",
    "install:all": "npm install --prefix client"
  }
}
